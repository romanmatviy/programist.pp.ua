---
id: 2
title: "Laravel 11: Що нового? Повний огляд оновлень та змін в архітектурі"
excerpt: "Огляд нових можливостей Laravel 11. Спрощена структура проекту, нова конфігурація посередників у bootstrap/app.php, база даних SQLite за замовчуванням та революційний WebSocket-сервер Laravel Reverb."
date: "2025-01-10"
updatedAt: "2025-01-12"
author: "Роман Матвій"
authorRole: "Backend Developer"
authorBio: "Разработчик с 8-летним опытом работы с Laravel и PHP. Специализируется на создании сложных бэкенд-систем."
image: "/images/blog/laravel.jpg"
tags: ["Laravel","PHP","Backend"]
readTime: "8"
relatedPosts: [1,4,6]
---

## Нова ера PHP розробки: Ласкаво просимо до Laravel 11

Laravel продовжує залишатися найпопулярнішим PHP-фреймворком у світі, і реліз Laravel 11 підтверджує цей статус. Ця версія приносить значне спрощення структури проекту, нові потужні інструменти та значні покращення продуктивності. Творці фреймворку зосередилися на тому, щоб зробити процес розробки максимально швидким і комфортним (Developer Experience — DX).

У цій статті ми детально розглянемо найважливіші зміни в Laravel 11 та пояснимо, як вони вплинуть на написання коду у ваших нових проектах.

## 1. Максимально спрощена структура каталогів

Найпомітніша зміна для розробників, які створюють новий проект на Laravel 11, — це кардинально зменшена кількість файлів за замовчуванням. Багато файлів конфігурації, провайдерів (Providers) та посередників (Middleware) було видалено або перенесено безпосередньо в ядро фреймворку.

* **Один провайдер замість багатьох:** Замість кількох сервіс-провайдерів у папці `app/Providers` тепер за замовчуванням залишається лише один — `AppServiceProvider`. Налаштування маршрутизації та обробки подій тепер відбуваються в центральному файлі конфігурації додатку.
* **Немає папки config за замовчуванням:** Так, ви все правильно зрозуміли! У новому проекті папка конфігурацій порожня. Всі опції налаштовуються через файл `.env`. Якщо вам все ж потрібно змінити дефолтні параметри, ви можете опублікувати потрібний конфіг за допомогою команди:

```bash
php artisan config:publish --all
```

* **Спрощений файл маршрутів:** API та WebSockets маршрути тепер не створюються за замовчуванням. Для їх додавання використовуються нові лаконічні команди:

```bash
php artisan install:api
php artisan install:broadcasting
```

## 2. Нова точка входу у файл bootstrap/app.php

Всі налаштування поведінки додатку, посередників (middleware) та винятків (exceptions) тепер сконцентровані в одному файлі `bootstrap/app.php`. Це значно спрощує глобальну конфігурацію:

```php
use Illuminate\Foundation\Application;
use Illuminate\Foundation\Configuration\Exceptions;
use Illuminate\Foundation\Configuration\Middleware;

return Application::configure(basePath: dirname(__DIR__))
    ->withRouting(
        web: __DIR__.'/../routes/web.php',
        commands: __DIR__.'/../routes/console.php',
        health: '/up',
    )
    ->withMiddleware(function (Middleware $middleware) {
        // Додавання глобальних посередників
    })
    ->withExceptions(function (Exceptions $exceptions) {
        // Кастомний рендеринг винятків
    })->create();
```

## 3. SQLite за замовчуванням

Для нових додатків Laravel 11 тепер використовує SQLite як базу даних за замовчуванням. Це дозволяє розробникам розпочати роботу над проектом миттєво після запуску `laravel new`, без необхідності встановлювати локальні сервери MySQL чи PostgreSQL або налаштовувати Docker-контейнери на початкових етапах. База даних створюється автоматично у вигляді звичайного файлу всередині проекту.

## 4. Новий інструмент Laravel Reverb

Однією з найцікавіших новинок стала інтеграція **Laravel Reverb** — першокласного, неймовірно швидкого WebSocket сервера, розробленого спеціально для додатків Laravel. Він повністю написаний на PHP і сумісний з протоколом Pusher. Reverb дозволяє легко масштабувати real-time функціонал вашого додатку без сторонніх платних сервісів.

## Висновок

Laravel 11 робить крок назустріч максимальній лаконічності коду та простоті масштабування. Нова структура може здатися незвичною на перший погляд, але вона значно прискорює створення та підтримку проектів. Оновлення до Laravel 11 — це чудовий спосіб підвищити продуктивність ваших розробників та додатку загалом.
